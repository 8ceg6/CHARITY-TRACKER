</head>
<body>

    <h1 style="color:green;">Donations For <%=@charity.name %></h1>

<table>
    <tr>
    <th> Donor </th>
    <th> Amount </th>
    <th> Date </th>
    </tr>
 <% @charity.donations.collect do |d|%>
    <% if d.donor.present?%>
   
    <tr>
        <td> <%= d.donor.name %>  </td> 
        <td> $<%= d.amount %> </td>
        <td> <%= d.created_at.strftime("%-m-%d-%y")%> </td>
    </tr>
    <%end%>
<%end%>
    </table>
    </body>
    </html>
    <h2><span style="color:red;" > $<%=@charity.donations.donor_total%></span></h2>
<%=  button_to "Submit a Donation", charity_donations_new_path(@charity) %>
<%=  link_to "All Charities", charities_path%>
